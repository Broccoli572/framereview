<?php

use Illuminate\Foundation\Testing\RefreshDatabase;

 Pest->configure()->uses(\Tests\TestCase::class);

//
// Alternatively, use Laravel's built-in RefreshDatabase trait
// Pest->configure()->uses(RefreshDatabase::class);
//
